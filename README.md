# 📚 Telegram Bot - Passive Learning (TypeScript)

Telegram бот для поддержания теоретических знаний в различных областях. Бот автоматически генерирует вопросы с помощью OpenAI и отправляет их пользователям по расписанию. Написан на TypeScript для улучшенной типизации и надежности.

## 🚀 Возможности

- **Автоматическая генерация вопросов** с помощью OpenAI API
- **Три типа вопросов**:
  - `singleChoice` - один правильный ответ
  - `multiChoice` - несколько правильных ответов  
  - `textInputQuestion` - текстовый ответ
- **Персонализированное обучение** - выбор сферы деятельности
- **Гибкое расписание** - настройка частоты получения вопросов
- **История ответов** - просмотр отвеченных вопросов
- **Автоматическая очистка** сообщений через 20 секунд
- **Cron-задачи** для автоматической отправки вопросов
- **TypeScript** - строгая типизация и лучшая IDE поддержка

## 🛠️ Технологический стек

- **Node.js + TypeScript** - runtime среда с строгой типизацией
- **Telegraf** - фреймворк для Telegram ботов
- **Prisma** - ORM для работы с базой данных
- **SQLite** - база данных
- **OpenAI API** - генерация вопросов
- **node-cron** - планировщик задач

## 📦 Установка и настройка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd bot-passive-learning
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка переменных окружения
Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
# Telegram Bot Token
BOT_TOKEN=your_telegram_bot_token_here

# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Database URL
DATABASE_URL="file:./dev.db"

# Node Environment
NODE_ENV=development
```

### 4. Настройка базы данных
```bash
# Генерация Prisma клиента
npm run db:generate

# Применение миграций
npm run db:push
```

### 5. Запуск бота
```bash
# Для разработки
npm run dev

# Сборка проекта
npm run build

# Для продакшена
npm start
```

## 🤖 Использование бота

### Основные команды:
- `/start` - Начать работу с ботом
- `/profile` - Просмотр и редактирование профиля
- `/question` - Получить новый вопрос
- `/history` - История ответов
- `/help` - Справка

### Процесс работы:
1. **Регистрация** - пользователь выбирает сферу деятельности и частоту вопросов
2. **Получение вопросов** - автоматически по расписанию или по команде
3. **Ответы** - в зависимости от типа вопроса
4. **Обратная связь** - уведомление о правильности ответа
5. **История** - просмотр всех отвеченных вопросов

## 📅 Расписание отправки вопросов

- **Раз в день** - 9:00 AM
- **Раз в 2 дня** - 10:00 AM 
- **Раз в 4 дня** - 11:00 AM
- **Раз в неделю** - воскресенье, 12:00 PM

## 🗂️ Структура проекта

```
src/
├── index.ts                 # Основной файл бота
├── database.ts              # Подключение к базе данных
├── types/
│   └── index.ts             # TypeScript типы и интерфейсы
├── handlers/
│   ├── userHandlers.ts      # Обработчики пользовательских команд
│   └── questionHandlers.ts  # Обработчики вопросов
└── services/
    ├── openaiService.ts     # Интеграция с OpenAI
    ├── cronService.ts       # Планировщик задач
    └── messageCleanupService.ts # Очистка сообщений
prisma/
└── schema.prisma            # Схема базы данных
dist/                        # Скомпилированные JS файлы
```

## 🗄️ Схема базы данных

### User
- `telegramId` - ID пользователя в Telegram
- `field` - сфера деятельности
- `frequency` - частота вопросов
- `isActive` - включены ли автоматические вопросы

### Question
- `type` - тип вопроса
- `question` - текст вопроса
- `options` - варианты ответа (JSON)
- `correctAnswer` - правильный ответ (JSON)
- `explanation` - объяснение
- `expiresAt` - время удаления (30 дней)

### Answer
- `userAnswer` - ответ пользователя (JSON)
- `isCorrect` - правильность ответа

### MessageChain
- `messageIds` - ID сообщений для удаления (JSON)
- `scheduledDeleteAt` - время удаления

## 🔧 Дополнительные команды

```bash
# Разработка
npm run dev          # Запуск с hot reload
npm run build        # Компиляция TypeScript

# Управление базой данных
npm run db:generate  # Генерация клиента Prisma
npm run db:push      # Применение схемы к БД
npm run db:migrate   # Запуск миграций
npm run db:studio    # Открытие Prisma Studio
```

## 🚨 Важные замечания

1. **Токен бота** - получите у @BotFather в Telegram
2. **OpenAI API** - требуется платный аккаунт для генерации вопросов
3. **Ограничения Telegram** - соблюдайте лимиты API для избежания блокировки
4. **Хранение данных** - вопросы хранятся 30 дней, затем автоматически удаляются
5. **Очистка сообщений** - цепочки сообщений удаляются через 20 секунд
6. **TypeScript** - все типы определены в `src/types/index.ts`

## 🎯 Преимущества TypeScript

- **Строгая типизация** - меньше ошибок во время выполнения
- **Лучшая IDE поддержка** - автодополнение и рефакторинг
- **Самодокументация** - типы служат документацией
- **Раннее обнаружение ошибок** - на этапе компиляции
- **Улучшенная поддержка рефакторинга** - безопасные изменения кода

## 🤝 Вклад в проект

1. Fork проекта
2. Создание feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Открытие Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте логи на наличие ошибок
2. Убедитесь, что все переменные окружения настроены правильно
3. Проверьте подключение к интернету и доступность API
4. Убедитесь, что TypeScript компилируется без ошибок

## 🔄 Обновления

- v1.0.0 - Базовая функциональность на TypeScript
- Планируемые улучшения:
  - Добавление изображений к вопросам
  - Система рейтингов и достижений
  - Экспорт истории ответов
  - Мультиязычность
  - Улучшенная типизация и валидация
